#!/usr/bin/env node

const {args, con} = require('./bootstrap');
let youtube = require("../external/youtube");

youtube.getChannels().then(result => {
    // console.log(JSON.stringify(result));
    console.log(result);
    process.exit();
}).catch(e => {
    console.log(e);
})
